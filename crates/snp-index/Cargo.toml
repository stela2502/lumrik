[package]
name = "snp-index"
version.workspace = true
edition = "2024"

description = "Fast SNP indexing and read matching with scdata integration (cell × SNP sparse matrices)"
license = "MIT"
repository = "https://github.com/yourname/snp-index"
readme = "README.md"
keywords = ["bioinformatics", "snp", "genomics", "single-cell", "rust"]
categories = ["science", "data-structures"]

[dependencies]
anyhow = "1.0.102"
flate2 = "1"
rust-htslib.workspace = true

gtf_splice_index = { path = "../gtf_splice_index" }
mapping_info = { path = "../mapping_info" }
scdata = { path = "../scdata" }

