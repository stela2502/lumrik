// Intentionally empty: the public evidence types live in cellrep/mod.rs so the
// complete per-cell data model is visible in one place.  This file exists only
// to reserve the hierarchy for future evidence-specific helpers without
// fragmenting the type model.
